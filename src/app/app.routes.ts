import { Routes } from '@angular/router';
import { ShowUserComponent } from './pages/show-user/show-user.component';

export const routes: Routes = [

    { path: 'user-list', component: ShowUserComponent }
];
